#!/bin/bash

source ./bin/check || exit 1

if [ -z "${1}" ]
then
  >&2 echo "No database specified"
  exit 1
fi

if [ ! -f "${2}" ]
then
  >&2 echo "File not found: ${2}"
  exit 1
fi

sqlplus -F -L -S /@${1} @${2}
